import type { shadowKeys } from "./index.type";

export const shadowObject: Record<shadowKeys, string> = {
    default: "0px 4px 6px rgba(0, 0, 0, 0.1)",
    hover: "0px 6px 8px rgba(0, 0, 0, 0.15)",
};
